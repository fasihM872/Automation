# Email Templates

`pitch_email.html` is a Jinja template used by `content.py`.

Available template variables:

- `intro_paragraphs`
- `template_name`
- `template_url`
- `preview_src`
- `sender_name`
- `sender_email`
